# operation_name: GetProducts
query GetProducts {
  topProducts(first: 5) {
    ...ProductInfo
  }
}

fragment ProductInfo on Product {
  name
  price
  reviews @mock(hint: "positive reviews") {
    id
    body
  }
}
