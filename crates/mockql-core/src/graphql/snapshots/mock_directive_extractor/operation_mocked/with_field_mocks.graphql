query GetInfo @mock(hint: "operation-level hint") {
  me @mock(hint: "mocked user") {
    id
    name
  }
  topProducts(first: 5) {
    reviews @mock(hint: "positive reviews") {
      body
    }
  }
}
