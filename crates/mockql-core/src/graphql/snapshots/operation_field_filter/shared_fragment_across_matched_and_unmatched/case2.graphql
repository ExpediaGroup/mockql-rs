# mock_paths: ["topProducts.reviews"]
query GetProducts {
  recommendedProducts {
    name
    reviews {
      ...ReviewFields
    }
  }
  topProducts(first: 5) {
    name
    reviews @mock(hint: "positive reviews") {
      ...ReviewFields
    }
  }
}

fragment ReviewFields on Review {
  id
  body
  rating
}