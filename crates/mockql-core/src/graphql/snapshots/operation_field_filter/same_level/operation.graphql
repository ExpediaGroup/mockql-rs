# mock_paths: ["me.name", "me.username", "me.reviews"]
# operation_name: GetUser
query GetUser {
  me {
    id
    name @mock(hint: "celebrity name")
    username @mock(hint: "unique username")
    reviews @mock {
      id
      body
    }
  }
}
